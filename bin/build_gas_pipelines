#!/bin/bash

mkdir -p build

cat build/all_pipelines.ndjson \
  | ndjson-filter 'd.properties.type === "gas"' \
  | ndjson-reduce \
  | ndjson-map '{type: "FeatureCollection", features: d}' \
  > build/gas_pipelines.geojson
